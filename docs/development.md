# Development

The pipeline is:
1. features/bug-fixes are thought up and put into the release timeline in `PLANNED.md`
2. once all changes are finalized, move them into `docs/wip.md`. Modify lines to include a checklist
3. Work on features. Generally each line should have its own commit/commits
4. Once everything is checked, tag and release.