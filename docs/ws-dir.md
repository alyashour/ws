# The WS Dir

Somewhere on the machine there must be a `.ws` dir that stores all of workspace's data and config.
This is a live document of what's contained in `.ws`.

```
.ws
├── data
│   └── tasks
│       ├── default.yaml
│       └── tasks.yaml
└── sync_conf.yaml
```